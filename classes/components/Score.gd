# ABOUTME: Score component — tracks player points as singleton on DeusWorld
# ABOUTME: Pure data container read/written by ScoringPipeline

class_name Score extends DefaultComponent
@export var value: int = 0
